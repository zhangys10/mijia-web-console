# AGENTS.md

本文件适用于整个仓库，用于约束在本项目中工作的开发者与自动化 Agent。子目录如有更具体的 `AGENTS.md`，以离目标文件最近的规范为准。

## 项目基线

- 运行时：Node.js `>=22.13.0`，依赖必须通过 `npm` 和已提交的 `package-lock.json` 管理。
- 应用：Next.js 16、React 19、Vinext/Vite，部署目标为 Cloudflare Workers。
- 代码：应用与领域逻辑使用 TypeScript；测试使用 Node.js Test Runner。
- 开始改动前先阅读 `README.md`；涉及设备、开关、照明或拓扑时，还必须阅读 `docs/device-management-design.md`。

## 目录职责

- `app/`：页面、样式和 Route Handlers。客户端组件只负责交互与展示，不得持有小米会话凭据。
- `lib/`：米家云访问、MIoT 能力解析和设备领域模型。优先把可测试的规则写成无副作用函数。
- `worker/`：Cloudflare Worker 入口及运行时适配，避免在此重复领域逻辑。
- `tests/`：单元、API、构建产物和响应式回归测试。修复缺陷时先补能复现问题的测试。
- `docs/`：跨模块设计约束。实现改变既有业务语义时同步更新文档。
- `dist/`、`.next/`、`.wrangler/`：生成目录，不直接编辑或提交。

## 标准开发流程

1. **确认范围**：检查工作区状态和相关实现、测试、设计文档；保留用户已有改动，不重置或覆盖无关文件。
2. **建立证据**：修复问题前先复现；新增能力前明确输入、输出、错误行为和安全边界。不得用猜测补全小米或 MIoT 未公开语义。
3. **最小实现**：只修改完成目标所需的代码，复用已有模型和工具函数，避免顺手重构或引入无关依赖。
4. **同步测试**：新增行为、修复缺陷、修改 API 合约或领域规则时，必须新增或更新相应测试。
5. **运行验证**：先运行最相关的定向测试，再通过仓库质量门禁。若受环境限制无法执行某项检查，交付时明确说明原因和风险。
6. **检查差异**：提交结果前审阅 `git diff`，确认没有凭据、调试输出、生成文件或无关格式化改动。

## 实现约束

### TypeScript 与模块边界

- 遵循现有 TypeScript、ES Module 和 ESLint 风格；不要通过 `any`、无依据的类型断言或关闭规则来绕过类型问题。
- `lib/` 中的领域函数应尽量确定、可组合、可独立测试；网络访问和框架对象留在边界层。
- 不要为兼容假设中的旧行为保留重复实现。确需兼容时，用测试记录兼容范围。
- 新增依赖前确认标准库和现有依赖不能满足需求；依赖变化必须同时提交 `package.json` 与 `package-lock.json`。

### 米家与设备领域

- 所有设备索引、匹配、缓存和去重以 `homeId` 为第一层作用域，禁止跨家庭关联。
- 派生 DID、设备类别、控制关系和证据等级必须遵循 `docs/device-management-design.md`；不要用名称猜测覆盖型号、规格或实时属性。
- MIoT 请求使用真实物理 DID 和明确的 `siid`/`piid`；单个规格或属性批次失败不应拖垮整个设备同步。
- 未知、冲突或读取失败的信息保持 `unknown`，不要伪装成已确认状态。

### API 与安全

- API 在访问小米云前校验方法、参数、标识符和请求体，并返回稳定、可测试的错误码。
- `XIAOMI_SESSION_SECRET`、`serviceToken`、`ssecurity`、Cookie、二维码状态和真实账号数据不得进入源码、日志、错误消息、测试夹具或提交记录。
- 会话字段只允许在服务端边界处理；客户端响应仅返回实现界面所需的最小数据。
- 不提交任何 `.env*` 文件。测试凭据必须是明显虚构、局部设置且不可用于真实环境的值。

### UI 与样式

- 保持桌面端和移动端可用；交互控件应具备可访问名称、键盘语义和清晰的加载、空、离线及失败状态。
- 优先复用现有组件结构和 CSS 约定。修改响应式布局时同步更新相应回归测试。
- 不用乐观界面掩盖控制失败；设备状态不确定时明确展示未知或待确认。

## 测试与质量门禁

代码改动默认必须通过：

```bash
npm run typecheck
npm run lint
npm test
```

- `npm test` 会先构建应用，再运行 `tests/*.test.mjs`，用于验证 Cloudflare Worker 构建产物和全部测试。
- 开发中可先用 `npm run test:unit -- tests/<相关文件>.test.mjs` 运行定向测试，但它不能替代最终的 `npm test`。
- 纯文档改动可以不运行构建和测试，但仍需检查 Markdown 内容、链接和 `git diff`。
- 不得删除、跳过或放宽测试来让失败消失，除非产品行为已明确改变，并同时更新实现依据和相关文档。

## Git 与交付

- 不执行破坏性 Git 操作，不丢弃用户改动；不要修改与任务无关的文件。
- 除非明确要求，不创建提交、不推送分支、不发布部署。
- 交付说明应包含：完成内容、关键行为变化、执行过的验证，以及仍存在的限制或未验证项。
